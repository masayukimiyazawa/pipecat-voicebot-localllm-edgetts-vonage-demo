[project]
name = "pipecat-voicebot-unifiedvideoapi-test"
version = "0.1.0"
description = "Pipecat voice bot using Vonage Unified Video Audio Connector + LM Studio + Bark TTS"
requires-python = ">=3.11"
dependencies = [
    "pipecat-ai[kokoro,mlx-whisper,openai,piper,runner,silero,websocket,whisper]>=1.4.0",
    "vonage>=3.3.1",
    "vonage-video",
    "python-dotenv",
    "uvicorn[standard]",
    "fastapi",

]
